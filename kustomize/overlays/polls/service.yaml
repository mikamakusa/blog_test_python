apiVersion: v1
kind: Service
metadata:
  name: polls
spec:
  selector:
    app: polls
  ports:
    - protocol: TCP
      port: 8000
      nodePort: 32318
      targetPort: 8000
  type: LoadBalancer
  